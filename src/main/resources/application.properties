spring.application.name=cashmachine

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cash_machine
spring.datasource.username=root
spring.datasource.password=32213221
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect


# Change Spring Boot's port if needed
server.port=8081

logging.level.org.springframework=DEBUG


#spring.jpa.hibernate.ddl-auto=create
# This ensures the tables are recreated each time the app starts.
